seed_everything: 42
trainer:
  gradient_clip_val: 1.0
  gradient_clip_algorithm: "norm"
  max_epochs: 120
  accelerator: auto
  devices: auto
  logger:
    class_path: lightning.pytorch.loggers.tensorboard.TensorBoardLogger
    init_args:
      save_dir: "logs/"
      name: "imagenet_8_cross_attend_share_grouped_1024bs_wd_1_grad_clip_nojitter_adamw_1e-4"
      version: null
      default_hp_metric: true
  accumulate_grad_batches: 16
model:
  learning_rate: 0.0001
  weight_decay: 0.1
  num_freq_bands: 64
  max_freq: 224
  depth: 8
  num_latents: 512
  latent_dim: 1024
  self_per_cross_attn: 6
  weight_share: true
  compile: true
  decay_all: false
  lamb: false
  label_smoothing: 0.0
data:
  batch_size: 8
  data_dir: "data/imagenet"